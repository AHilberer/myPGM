[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "myPGM"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.package-data]
"myPGM" = ["ui/*.qss", "resources/*", "resources/icons/*"]